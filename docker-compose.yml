version: "3.9"

services:
  app:
    build: ./web
    ports:
      - "3004:80"
    restart: unless-stopped
    environment:
      DB_HOST: 192.168.1.21
      DB_NAME: door_dev
      DB_USER: root
      DB_PASS: Superce11ular!